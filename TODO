Providers to implement:

	GeoClue
	Guess location by airport
	    wget http://weather.noaa.gov/pub/data/observations/metar/decoded/ESSA.TXT -O - 2>/dev/null | head -n 1 |
                cut -d ')' -f 2 | cut -d ' ' -f 2,3 | sed -e 's:-:.:g' | sed -e 's:\([0-9.]*\)\(S\|W\):-\1:g'
	Guess location by guessing city from to where the symlink /etc/locattime refers
	Guess location by guessing town from postal code and country, if country is omitted
	  use country of the city guessed from /etc/locattime
	GNOME clock and similar applets

If two instances of locateme is running one
should be idle wait for updates from the other.

Add option to omit the used method.

